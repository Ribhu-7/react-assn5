import React from 'react'

const Purecomponent = () => {
  return (
    <div><h1>Pure Component</h1>
    <p>A React component is considered pure if it renders the same output for the same state and props. For this type of class component, React provides the PureComponent base class. Class components that extend the React.PureComponent class are treated as pure components.</p></div>
    
  )
}

export default Purecomponent